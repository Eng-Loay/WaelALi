PORT=5000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=wael_ali_math
ADMIN_EMAIL=admin@waelalimath.com
ADMIN_PASSWORD=admin123
ADMIN_NAME=مستر وائل علي
# Must stay unchanged on the server after first set — changing it logs everyone out.
# If omitted, the backend uses a fixed built-in fallback (sessions survive restarts).
JWT_SECRET=wael-ali-math-dev-secret-change-in-production
# Session length in hours (default 720 = 30 days)
JWT_EXPIRES_HOURS=720
